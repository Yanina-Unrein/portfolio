import { Injectable, signal } from '@angular/core';
import { UserProfile, UserPresence, UserActivity, UserStatus } from '@/app/core/models/user';

@Injectable({
  providedIn: 'root'
})
export class UserService {
  
}
